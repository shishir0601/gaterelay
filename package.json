{
  "name": "gaterelay",
  "version": "1.0.0",
  "description": "Peer-to-peer campus gate pickup relay: matches parcel/food requests to people already walking to the gate, with EDF interval scheduling, reputation-gated trust, and OTP-style handoff codes. Zero dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/match.test.js test/rate-limiter.test.js test/api.test.js"
  },
  "engines": { "node": ">=18" },
  "keywords": ["campus", "matching", "scheduling", "trust", "zero-dependency"],
  "license": "MIT"
}
